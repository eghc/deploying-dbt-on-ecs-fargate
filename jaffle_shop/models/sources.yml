version: 2

sources:
    - name: utils
      description: This is a replica of the Postgres database used by our app
      database: raw
      schema: utils
      tables:
          - name: dates
          - name: numbers